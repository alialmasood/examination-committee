'use client';

/**
 * إعدادات نظام الحسابات (سوبر أدمن) —
 * الصفحة جاهزة؛ المحتوى يُضاف لاحقاً (مثل جدول الأقساط من مصدر قابل للتعديل).
 */
export default function PlatformAccountsPage() {
  return null;
}
