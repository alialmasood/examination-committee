'use client';
/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from 'next/link'; import {useEffect,useState} from 'react'; import SuppliersNav from '../SuppliersNav';
const api='/api/accounts/supplier-payments';
export function PaymentList(){const[r,setR]=useState<any>();useEffect(()=>{fetch(api).then(x=>x.json()).then(setR)},[]);return <main dir="rtl" className="p-6 max-w-7xl mx-auto"><div className="flex justify-between"><h1 className="text-2xl font-bold">دفعات الموردين</h1><Link className="bg-blue-600 text-white rounded px-4 py-2" href="payments/new">دفعة جديدة</Link></div><SuppliersNav/><table className="w-full bg-white shadow rounded text-right"><thead><tr><th className="p-3">الرقم</th><th>المورد</th><th>التاريخ</th><th>المبلغ</th><th>الطريقة</th><th>الحالة</th></tr></thead><tbody>{r?.data?.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3"><Link className="text-blue-600" href={`payments/${x.id}`}>{x.payment_number}</Link></td><td>{x.supplier_name_ar}</td><td>{x.payment_date}</td><td>{x.amount}</td><td>{x.payment_method}</td><td>{x.status}</td></tr>)}</tbody></table></main>}
export function PaymentNew(){const[o,setO]=useState<any>(),[f,setF]=useState<any>({payment_date:new Date().toISOString().slice(0,10),payment_method:'CASH',amount:'',allocations:[]}),[msg,setMsg]=useState('');useEffect(()=>{fetch(`${api}/options`).then(x=>x.json()).then(setO)},[]);const change=(k:string,v:any)=>setF({...f,[k]:v});async function save(){const r=await fetch(api,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)}).then(x=>x.json());if(!r.data)return setMsg(r.error||'تعذر الحفظ');const pre=await fetch(`${api}/${r.data.id}/preview-allocation`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({amount:f.amount,mode:'auto'})}).then(x=>x.json());if(!pre.data)return setMsg(pre.error||'تعذر معاينة التخصيص');const up=await fetch(`${api}/${r.data.id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({version:r.data.version,updated_at:r.data.updated_at,allocations:pre.data.allocations})}).then(x=>x.json());if(confirm('هل تريد ترحيل الدفعة الآن؟'))await fetch(`${api}/${r.data.id}/post`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({version:up.data.version,updated_at:up.data.updated_at})});location.href=`/accounts/suppliers/payments/${r.data.id}`;}return <main dir="rtl" className="p-6 max-w-3xl mx-auto"><h1 className="text-2xl font-bold">دفعة مورد جديدة</h1><SuppliersNav/><div className="grid gap-3 bg-white shadow rounded p-5"><input className="border p-2" placeholder="معرّف حساب المورد" value={f.supplier_account_id||''} onChange={e=>change('supplier_account_id',e.target.value)}/><input className="border p-2" type="date" value={f.payment_date} onChange={e=>change('payment_date',e.target.value)}/><input className="border p-2" type="number" placeholder="المبلغ" value={f.amount} onChange={e=>change('amount',e.target.value)}/><select className="border p-2" value={f.payment_method} onChange={e=>change('payment_method',e.target.value)}><option value="CASH">نقدي</option><option value="BANK">مصرفي</option></select>{f.payment_method==='CASH'?<select className="border p-2" onChange={e=>{const s=o?.data?.open_sessions?.find((x:any)=>x.id===e.target.value);setF({...f,cash_box_session_id:e.target.value,cash_box_id:s?.cash_box_id})}}><option>اختر الجلسة المفتوحة</option>{o?.data?.open_sessions?.map((x:any)=><option key={x.id} value={x.id}>{x.cash_box_name_ar} — {x.session_date}</option>)}</select>:<select className="border p-2" onChange={e=>change('bank_account_id',e.target.value)}><option>اختر الحساب المصرفي</option>{o?.data?.bank_accounts?.map((x:any)=><option key={x.id} value={x.id}>{x.code} — {x.account_name_ar}</option>)}</select>}<textarea className="border p-2" placeholder="الوصف" onChange={e=>change('description',e.target.value)}/><button className="bg-blue-600 text-white rounded p-2" onClick={save}>حفظ، معاينة وترحيل</button>{msg&&<p className="text-red-600">{msg}</p>}</div></main>}
export function PaymentDetail({id}:{id:string}){const[r,setR]=useState<any>();useEffect(()=>{fetch(`${api}/${id}`).then(x=>x.json()).then(setR)},[id]);async function act(x:string){if(x==='void'&&!confirm('تأكيد إلغاء الدفعة؟'))return;await fetch(`${api}/${id}/${x}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({version:r.data.version,updated_at:r.data.updated_at,reason:x==='void'?'إلغاء من الشاشة':undefined})});location.reload()}const p=r?.data;return <main dir="rtl" className="p-6 max-w-3xl mx-auto"><h1 className="text-2xl font-bold">تفاصيل الدفعة</h1><SuppliersNav/>{p&&<section className="bg-white shadow rounded p-5 space-y-2"><p>{p.payment_number} · {p.amount} IQD · {p.status}</p><p>{p.description}</p><h2 className="font-bold">التخصيصات</h2>{p.allocations.map((x:any)=><p key={x.id}>{x.invoice_number}: {x.allocated_amount}</p>)}{p.status==='DRAFT'&&<button className="bg-green-600 text-white p-2 rounded" onClick={()=>act('post')}>ترحيل</button>}{p.status!=='VOID'&&<button className="bg-red-600 text-white p-2 rounded mr-2" onClick={()=>act('void')}>إلغاء</button>}<Link className="text-blue-600" href={`/accounts/suppliers/payments/${id}/print`}>طباعة</Link></section>}</main>}
