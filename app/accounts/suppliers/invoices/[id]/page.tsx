'use client';
/* eslint-disable @typescript-eslint/no-explicit-any */
import{useEffect,useState}from'react';import{useParams}from'next/navigation';import Link from'next/link';
export default function InvoiceDetail(){const{id}=useParams<{id:string}>();const[i,setI]=useState<any>();const[error,setError]=useState('');const load=()=>fetch(`/api/accounts/supplier-invoices/${id}`).then(r=>r.json()).then(x=>x.success?setI(x.data):setError(x.message));// eslint-disable-next-line react-hooks/exhaustive-deps -- تحميل أولي بالمعرّف
useEffect(()=>{void load()},[id]);const action=async(a:string)=>{if(a==='void'&&!window.confirm('هل تريد إلغاء الفاتورة؟'))return;const body={version:i.version,updated_at:i.updated_at,...(a==='void'?{reason:prompt('سبب الإلغاء')||''}:{})};const r=await fetch(`/api/accounts/supplier-invoices/${id}/${a}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const x=await r.json();if(x.success)void load();else setError(x.message)};if(!i)return <main className="p-6" dir="rtl">جاري التحميل...</main>;return <main dir="rtl" className="p-6 max-w-4xl mx-auto"><div className="flex justify-between"><div><h2 className="text-2xl font-bold">فاتورة {i.invoice_number}</h2><p>{i.supplier_name_ar} — {i.status}</p></div><div className="flex gap-2"><Link href={`/accounts/suppliers/invoices/${id}/print`}>طباعة</Link>{i.status==='DRAFT'&&<><button onClick={()=>action('post')}>ترحيل</button><button onClick={()=>action('void')}>إلغاء</button></>}{i.status==='POSTED'&&<button onClick={()=>action('void')}>إلغاء وعكس</button>}</div></div>{error&&<p className="text-red-600">{error}</p>}<section className="grid md:grid-cols-2 gap-4 bg-white shadow p-6 rounded-xl mt-6"><p>رقم المورد: {i.supplier_invoice_number}</p><p>التاريخ: {i.invoice_date}</p><p>المصروف: {i.expense_gl_code} — {i.expense_gl_name_ar}</p><p>الإجمالي: <b>{i.total_amount}</b></p><p>الخصم: {i.discount_amount}</p><p>الضريبة: {i.tax_amount}</p><p className="md:col-span-2">الوصف: {i.description}</p></section></main>}
