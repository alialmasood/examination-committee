'use client';
/* eslint-disable @typescript-eslint/no-explicit-any */
import{useEffect,useState}from'react';import Link from'next/link';import SuppliersNav from'../SuppliersNav';
export default function Invoices(){const[r,setR]=useState<any>();const[page,setPage]=useState(1);useEffect(()=>{fetch(`/api/accounts/supplier-invoices?page=${page}`).then(x=>x.json()).then(setR)},[page]);return <main dir="rtl" className="p-6 max-w-7xl mx-auto"><div className="flex justify-between"><h2 className="text-2xl font-bold">فواتير الموردين</h2><Link className="bg-blue-600 text-white rounded px-4 py-2" href="/accounts/suppliers/invoices/new">فاتورة جديدة</Link></div><SuppliersNav/><table className="w-full bg-white shadow rounded-xl text-right"><thead><tr className="bg-gray-50"><th className="p-3">الرقم</th><th>المورد</th><th>التاريخ</th><th>المبلغ</th><th>الحالة</th></tr></thead><tbody>{r?.data?.map((x:any)=><tr className="border-t" key={x.id}><td className="p-3"><Link className="text-blue-600" href={`/accounts/suppliers/invoices/${x.id}`}>{x.invoice_number}</Link></td><td>{x.supplier_name_ar}</td><td>{x.invoice_date}</td><td>{x.total_amount}</td><td>{x.status}</td></tr>)}</tbody></table><div className="mt-4"><button disabled={page===1} onClick={()=>setPage(page-1)}>السابق</button> <button onClick={()=>setPage(page+1)}>التالي</button></div></main>}
