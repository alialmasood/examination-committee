'use client';
import {useEffect,useState}from'react';export default function Print({params}:{params:Promise<{id:string}>}){const[r,setR]=useState<any>();useEffect(()=>{params.then(({id})=>fetch(`/api/accounts/direct-expenses/${id}`).then(x=>x.json()).then(setR))},[params]);const x=r?.data;return <main dir="rtl" className="p-10 max-w-3xl mx-auto"><button className="print:hidden border p-2" onClick={()=>print()}>طباعة</button>{x&&<article><h1 className="text-2xl font-bold">كلية الشرق الجامعة</h1><h2>سند مصروف مباشر — {x.expense_number}</h2><p>{x.expense_date} · {x.beneficiary_name} · {x.amount} IQD</p><p>{x.description}</p><div className="grid grid-cols-3 mt-20"><p>المحاسب: __________</p><p>المستفيد: __________</p><p>المعتمد: __________</p></div></article>}</main>}
