'use client';
/* eslint-disable @typescript-eslint/no-explicit-any */
import{useEffect,useState}from'react';import SuppliersNav from'../SuppliersNav';
export default function Page(){const[r,setR]=useState<any>(),[f,setF]=useState({code:'',name_ar:''});const load=()=>fetch('/api/accounts/direct-expense-types').then(x=>x.json()).then(setR);useEffect(()=>{load()},[]);async function add(){await fetch('/api/accounts/direct-expense-types',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});setF({code:'',name_ar:''});load()}async function off(x:any){await fetch(`/api/accounts/direct-expense-types/${x.id}/deactivate`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({version:x.version,updated_at:x.updated_at})});load()}return <main dir="rtl" className="p-6 max-w-5xl mx-auto"><h1 className="text-2xl font-bold">أنواع المصروفات</h1><SuppliersNav/><div className="flex gap-2 mb-4"><input className="border p-2" placeholder="الرمز" value={f.code} onChange={e=>setF({...f,code:e.target.value})}/><input className="border p-2" placeholder="الاسم العربي" value={f.name_ar} onChange={e=>setF({...f,name_ar:e.target.value})}/><button className="bg-blue-600 text-white rounded px-4" onClick={add}>إضافة</button></div><table className="w-full bg-white shadow rounded"><thead><tr><th>الرمز</th><th>الاسم</th><th>الحالة</th><th></th></tr></thead><tbody>{r?.data?.map((x:any)=><tr className="border-t" key={x.id}><td>{x.code}</td><td>{x.name_ar}</td><td>{x.is_active?'فعّال':'معطّل'}</td><td>{x.is_active&&<button className="text-red-600" onClick={()=>off(x)}>تعطيل</button>}</td></tr>)}</tbody></table></main>}
